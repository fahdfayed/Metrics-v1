import React from 'react';
import Features from '../components/Features';
import { SliderData } from '../data/SliderData';

const Contact = () => {
  return <Features slides={SliderData}/>;
};

export default Contact;
